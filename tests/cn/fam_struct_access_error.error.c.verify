return code: 0
cn/fam_struct_access_error.error.c:12:5: warning: CN pointer equality is not the same as C's (will not warn again). Please use `ptr_eq` or `is_null` (maybe `addr_eq`).
    O.x == I.x;
    ~~~~^~~~~~ 
cn/fam_struct_access_error.error.c:12:5: warning: Accessing flexible array member x on logical struct value is discouraged (will not warn again). FAM fields in logical structs represent only the base pointer, not array contents. Consider using pointer access (p->x) for array operations.
    O.x == I.x;
    ~^~ 
[1/1]: test -- pass
